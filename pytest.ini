[pytest]
; Both suites: the model/inference tests and the vendored generator's own
; contract tests. Excluding generator/tests let wrong-label generator
; regressions escape routine runs (audit H-13).
testpaths = tests generator/tests
addopts = -q
